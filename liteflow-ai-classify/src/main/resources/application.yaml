spring:
  profiles:
    active: @spring.profiles.active@
  application:
    name: liteflow-ai-classify

liteflow:
  rule-source: flow/flow.xml
  ai:
    enable: true
    base-packages: com.lunay.liteflow.ai.classify.node
    dashscope:
      apikey: ${classify.dashscope.apikey:your-dashscope-api-key-here}
